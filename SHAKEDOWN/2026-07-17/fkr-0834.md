# [SHAKEDOWN] fkr — 2026-07-17 08:34

**Target:** fkr (`fkr/SKILL.md`)
**Target HEAD:** 8a4e7c5 Remove XYZ relay system from repo history (last touch to fkr/SKILL.md itself: 33dd279 docs(fkr): fix SKILL.md script paths, backup claim, shared-module dep)
**Env:** Darwin 24.6.0 arm64 · bash 3.2.57(1)-release (arm64-apple-darwin24)
**Verdict:** [path bug reproduced] — the doc's script paths are unquoted, and this exact repo checkout lives at a path with a space in it (`.../GH Repos/fast-key-replacement-macos`), so the realistic invocation breaks today, not just in a contrived scenario.

## Static audit

`audit.sh` grades `.sh` invocation tokens and bundled `.sh` scripts only. fkr has neither — its bundled scripts are Python (`scripts/*.py`, living in the repo's top-level `scripts/`, *outside* `fkr/`) — so the tool returned a technically-true but uninformative "clean":

```
### Invocation paths (graded from SKILL.md)
(no .sh invocation tokens in SKILL.md — nothing to grade)

### Bundled-script hygiene
(no bundled .sh scripts — pure-prose skill; nothing to harden)

Verdict: [clean]
```

This is a coverage gap in the audit tool, not a clean bill of health for fkr — see below. Manually walking SKILL.md's Python invocations instead:

- fkr/SKILL.md:44 explicitly flags its own paths as a placeholder: *"the examples below use `path/to/repo/scripts/`"* — this is not a disguised hardcoded path (the classic shakedown bug); it's an openly-labeled template the invoking agent is expected to substitute with the real checkout path.
- 8 code-block lines (L86, 89, 97, 101, 104, 118, 126, 135) use that placeholder **unquoted**: `python3 path/to/repo/scripts/<name>.py ...`.
- Python's own module resolution is not CWD-sensitive here — `import replacements_common` at the top of `md_to_json.py` / `json_to_native.py` / `lint_replacements.py` / `json_to_apple_sqlite.py` works regardless of CWD, because Python inserts the invoked script's own directory into `sys.path[0]` automatically. No `BASH_SOURCE`-style self-location is needed on the Python side, and none is missing.
- No hardcoded absolute paths from the original dev machine were found anywhere in `scripts/`, `fkr/`, or `macOS/make-app.sh` (checked via grep for `/Users/`, `/home/`).

So the one real, checkable risk is: **does the invocation survive substitution + CWD variation**, given it's unquoted. That's what the live harness checks below.

## Live harness

Ran against a lightweight staged copy (`fkr/SKILL.md` + `scripts/*.py`) rather than the full repo — a straight `--target` on the repo root timed out the harness's per-scenario `cp -R`, because it would've copied `.git` history and Swift build artifacts eight times over. The staged copy mirrors exactly what SKILL.md describes (scripts alongside, not nested in `fkr/`), so the result is representative.

Invocation under test: `lint_replacements.py` with no args — a clean argparse usage error (`exit 2`, no filesystem side effects, doesn't touch the real `~/Library/KeyboardServices/TextReplacements.db`).

### Run A — as-realistically-run: `python3 scripts/lint_replacements.py`
*(the literal SKILL.md text, `python3 path/to/repo/scripts/lint_replacements.py`, is unrunnable by design — it's a labeled placeholder, not a copy-pasteable command. This row instead tests the realistic failure mode: a session that resolves the placeholder to a CWD-relative path instead of an absolute one.)*

```
control(CWD=skill)   found/err  exit 2   usage: lint_replacements.py [-h] input ...
foreign CWD          NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
nested CWD           NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
spaces in path        NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
project install       NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
user install           NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
symlinked install    NOT-FOUND  exit 2   can't open file 'scripts/lint_replacements.py'
stripped exec bit    found/err  exit 2   usage: lint_replacements.py [-h] input ...
```
-> Run A: **DISCOVERY BUG reproduced** — only works when CWD happens to already be the repo root. SKILL.md's own "Recommended Workflow" tells the user to run these commands *"From a project directory"* (e.g. `keyboard-replacements/`) — i.e. explicitly **not** the repo — so a naive relative substitution is close to guaranteed to break in the documented usage pattern.

### Run B — proposed anchor: `python3 {SKILL}/scripts/lint_replacements.py` (unquoted)

```
control(CWD=skill)   found/err  exit 2   usage: lint_replacements.py [-h] input ...
foreign CWD           found/err  exit 2   usage: lint_replacements.py [-h] input ...
nested CWD             found/err  exit 2   usage: lint_replacements.py [-h] input ...
spaces in path        NOT-FOUND  exit 2   can't open file '.../has': No such file or directory
project install        found/err  exit 2   usage: lint_replacements.py [-h] input ...
user install            found/err  exit 2   usage: lint_replacements.py [-h] input ...
symlinked install     found/err  exit 2   usage: lint_replacements.py [-h] input ...
stripped exec bit    found/err  exit 2   usage: lint_replacements.py [-h] input ...
```
-> Run B: **STILL FAILS** under "spaces in path" — confirmed by hand (see below) to be plain shell word-splitting: `python3 /tmp/.../has space/lite/scripts/lint_replacements.py` unquoted splits at the space, and `python3` tries to open `.../has` as the script. Quoting the path fixes it:

```
$ cmd='python3 "/tmp/.../has space/lite/scripts/lint_replacements.py"'
$ bash -c "$cmd"
usage: lint_replacements.py [-h] input
lint_replacements.py: error: the following arguments are required: input   # exit 2, found
```

This is not a contrived edge case for this repo specifically: the actual checkout under test lives at
`/Users/noelsaw/Documents/GH Repos/fast-key-replacement-macos` — **"GH Repos" has a space in it.** Any session that substitutes the real, unquoted absolute path into SKILL.md's examples as documented will hit exactly this failure today.

## Proposed patch

Two independent fixes, both against `fkr/SKILL.md`:

```diff
@@ -41,7 +41,8 @@
 ## Bundled Scripts
 
 The scripts live in the repository's top-level `scripts/` directory, not inside this skill's folder (`fkr/`). Invoke them with a path to that directory (the examples below use `path/to/repo/scripts/`).
+Always use the **absolute** path to the checkout, quoted — several real install locations for this repo (including the one used for this skill's own testing) contain spaces, e.g. `"~/Documents/GH Repos/fast-key-replacement-macos/scripts/native_to_json.py"`. An unquoted or CWD-relative path will silently resolve to the wrong file or fail outright depending on where the command is run from.
```

```diff
@@ -83,29 +84,29 @@
 ```bash
 mkdir -p keyboard-replacements
 
-python3 path/to/repo/scripts/native_to_json.py \
+python3 "path/to/repo/scripts/native_to_json.py" \
   --output keyboard-replacements/replacements.json
 
-python3 path/to/repo/scripts/json_to_md.py \
+python3 "path/to/repo/scripts/json_to_md.py" \
   keyboard-replacements/replacements.json \
   --output keyboard-replacements/replacements.md
 ```
```
*(repeat the same unquoted → quoted change for the remaining 6 occurrences at L97, L101, L104, L118, L126, L135 — same pattern throughout the "Recommended Workflow" and "Experimental Direct SQLite Write Workflow" sections.)*

No `BASH_SOURCE`-style self-location patch is needed on the Python side — `sys.path[0]` already anchors sibling imports to the invoked script's own directory, independent of CWD.

## What I could not verify

- This tests the **documented command under varied CWD/install/quoting conditions** — it does not instrument Claude Code's own skill-resolution internals, so it can't certify how the runtime itself would locate or substitute paths for `fkr/` if it were registered as an installed skill. As of this run, `fkr/` is **not** symlinked into `~/.claude/skills` or referenced by any project `.claude/skills` — it's a docs-only definition living in-repo, invoked (if at all) by an agent already working inside this checkout. That context makes the CWD risk somewhat academic for *this session*, but real the moment `fkr/` is packaged/symlinked for reuse elsewhere, or copy-pasted into another project's skill install.
- Did not exercise `json_to_apple_sqlite.py`'s `--apply` path, `native_to_json.py` against the real `TextReplacements.db`, or any script requiring real user data — out of scope for a discovery-only check, and avoided deliberately to stay read-only against live system state, not just the target skill.
- The harness's "stripped exec bit" scenario only strips `+x` from `*.sh` files; it has no effect on `.py` files invoked via `python3 <path>`, so that row's "found/err" result confirms exec-bit doesn't matter for this invocation style — not that the scenario was meaningfully exercised the way it would be for a bash-script skill.
- Did not test the Swift app's own script discovery (`PythonBridge.swift` / `macOS/make-app.sh`, which resolves bundled Python scripts from `Contents/Resources/scripts` at Finder-launch time) — that's app-internal resolution, not a Claude Code skill, and out of scope for `/shakedown`.

---

**Defaults in effect:** this report *proposes* the patch above rather than applying it, and lives per-repo under `SHAKEDOWN/` at this repo's root. Say the word if you'd rather I apply the quoting fix directly on a throwaway branch, or centralize shakedown reports across projects instead.
