# ── Local scratch / personal data (never committed) ─────────────────────
# Exported text replacements are personal data; keep the whole temp tree out.
temp/

# ── Build / OS artifacts ────────────────────────────────────────────────
.build/
.swiftpm/
macOS/dist/
.DS_Store
__pycache__/
*.pyc
keyboard text replacement
.claude/settings.json

# Aider Studio (auto-added)
REPO_MAP.md
.aider/
.xyz/
/relay-system

# codebase-memory MCP index artifacts
.codebase-memory/

# ask-self RAG artifacts (no longer committed)
ask_self/

# PDDA GitHub issue-state cache (regenerated by pdda-gh-refresh.sh)
.pdda-gh-state.tsv
